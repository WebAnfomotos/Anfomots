 // Función para recargar la página
 function recargarPagina() {
    location.reload();
  }

  // Llamar a la función para recargar cada 10 segundos (10000 milisegundos)
  setInterval(recargarPagina, 180000)